# This file is loaded inside the http{} block by Nginx.
# Only http-context directives are allowed here.
# worker_processes belongs in main context - set via ENV NGINX_WORKER_PROCESSES instead.
aio off;
